name: random_input_pad
description: "Random input pad"
version: 0.0.2
homepage: "https://github.com/MAHAulia/flutter_random_input_pad"

environment:
  sdk: '>=3.4.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0